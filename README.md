A fun couple of problems
========================

##Install dependencies
`npm install`

##Run tests
`gulp test`

##Launch the server
###(make sure to browserify first)
`gulp browserify-client`

`node app`


Then head to localhost:3000 and behold.